# stack Quest Lane

Repo-wide stack and guardrail obligations.

Quest source files in this lane live under lifecycle state directories such as
`done/`, `captured/`, or `triaged/`. Do not create top-level
`ABYSS-STACK-Q-*` aliases.
