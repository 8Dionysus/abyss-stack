# Design

The service is a four-tool no-network command bridge. It preserves owner
evidence and score-signal reports and requires source packets to prove
`network_touched=false` and `read_only=true`.
