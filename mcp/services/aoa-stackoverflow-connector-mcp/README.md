# aoa-stackoverflow-connector-mcp

Authenticated read-only access to already-built local StackOverflow packets.
Port `5437`, scope `mcp:aoa-stackoverflow-connector:read`, credential
`aoa-stackoverflow-connector-mcp-read-bearer-token`.

The documented hybrid route is withheld because the current owner CLI does not
implement `query-hybrid`.
