[pytest]
norecursedirs =
    .git
    __pycache__
    legacy
filterwarnings =
    # External FastAPI/Starlette dev-dependency warning; keep local test output
    # clean until the dependency lane moves to httpx2 deliberately.
    ignore:Using `httpx` with `starlette\.testclient` is deprecated; install `httpx2` instead\.:Warning
