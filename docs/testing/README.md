# testing

This district maps the active test surface for `abyss-stack`.

| Surface | Owns |
|---|---|
| [TEST_TOPOLOGY](TEST_TOPOLOGY.md) | human route map for test homes and failure routes |
| [test_inventory.json](test_inventory.json) | machine-readable coverage inventory for topology tests |

Executable commands live in `docs/validation/validation_lanes.json` and local
`AGENTS.md` cards, not in this inventory.
