#!/usr/bin/env python3
from __future__ import annotations

from pathlib import Path
import runpy
import sys

BACKEND = (Path(__file__).resolve().parent / "../mechanics/runtime-repair/parts/a2a-return-dry-run/aoa_a2a_return_closeout_dry_run.py").resolve()
sys.argv[0] = str(BACKEND)
runpy.run_path(str(BACKEND), run_name="__main__")
