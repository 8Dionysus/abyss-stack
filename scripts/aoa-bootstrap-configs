#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/../mechanics/config-projection/parts/bootstrap/aoa_bootstrap_configs.sh" "$@"
