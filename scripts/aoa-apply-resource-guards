#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/../mechanics/runtime-lifecycle/parts/start-stop/aoa_apply_resource_guards.sh" "$@"
