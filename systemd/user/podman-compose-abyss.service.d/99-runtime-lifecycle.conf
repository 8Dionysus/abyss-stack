[Service]
# Keep explicit stack teardown authoritative even when a distribution-wide
# user-service drop-in selects a cgroup-wide abort path.
Delegate=yes
KillMode=process
TimeoutStopFailureMode=terminate
