service:
  name: tos-graph
  port: 5410

source:
  corpus_index: ToS/derived-exports/tos_corpus_index.min.json

projection:
  mode: corpus_sync
  database: neo4j
  neo4j_shadow: true

ui:
  mode: api_plus_minimal_html
  corpus_first: true
  default_view: corpus-topology
