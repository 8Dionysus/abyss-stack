# Placeholder overlay that exercises AOA_EXTRA_COMPOSE_FILES without claiming a
# validated CPU tuning contract.
services:
  llama-cpp:
    labels:
      aoa.tuning.overlay: "llamacpp.cpu.placeholder"
