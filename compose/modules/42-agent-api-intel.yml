services:
  langchain-api:
    environment:
      EMBEDDINGS_PROVIDER: ovms
    depends_on:
      litellm:
        condition: service_healthy
      ollama:
        condition: service_started
      ovms:
        condition: service_healthy
