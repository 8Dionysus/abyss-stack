# Config Sync Boundary

Routes `scripts/aoa-sync-configs` and `docs/DEPLOYMENT.md`.

This part keeps source templates and deployed runtime configs distinct.

Use `docs/SOURCE_RUNTIME_PARITY_PACKET.md` when a source change must be checked
against both a synthetic runtime mirror and the deployed `Configs` mirror.
