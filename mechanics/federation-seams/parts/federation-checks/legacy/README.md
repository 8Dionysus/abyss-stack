# Federation Checks Legacy

This directory preserves compatibility detail that must not burden active
federation routes.

Use [`../docs/UPSTREAM_COMPATIBILITY.md`](../docs/UPSTREAM_COMPATIBILITY.md) as
the only active bridge. Open legacy detail only when changing or retiring an
upstream compatibility contract.
