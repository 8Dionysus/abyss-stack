# Agon Runtime Legacy

This is the provenance home for old flat Agon runtime files.

The files here were intentionally moved out of root-level folders because their
names carry old wave, seed, and phase scaffolding. They are retained so the
runtime history stays reviewable, but the active route is now the package card
one level up.

Use [INDEX.md](INDEX.md) to map old locations to current locations.

