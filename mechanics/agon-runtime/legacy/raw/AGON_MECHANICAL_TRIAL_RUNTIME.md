# Agon Mechanical Trial Runtime

Wave XIII adds local dry-run mechanical trial runs over the Wave XII duel kernel.

The runtime surface is intentionally bounded: local event logs only, no service activation, no daemon, no database migration, and no live verdict path.
