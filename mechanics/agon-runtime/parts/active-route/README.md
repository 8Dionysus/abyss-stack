# Active Route

This part owns the current agon-runtime route card surfaces:
`README.md`, `DIRECTION.md`, and `PROVENANCE.md`.

It does not own old kernel artifact names. Those stay in
`parts/legacy-runtime-kernels/` as a route to package-local legacy.
