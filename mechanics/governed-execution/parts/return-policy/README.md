# Return Policy

Routes `config-templates/Configs/agent-api/return-policy.yaml` and
`docs/GOVERNED_EXECUTION.md`.

Policy templates remain deployment inputs; active contracts live under
`parts/runtime-contracts/`.
