# Local Worker Path

Routes local worker pilot documentation and preserved W5/W6 lineage:
`docs/LANGGRAPH_PILOT.md` and inference-pilot legacy docs.

This part is route-only. It must not turn local worker execution into
ungoverned autonomy.
