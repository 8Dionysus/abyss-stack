# Governed Execution Landing Log

## 2026-08-13 - Stable external owner-request result identity

Separated the cross-owner identity of an admitted `aoa-agents` execution
request from the runtime-private path of its immutable materialized copy. The
terminal result now returns the stable `request_ref` plus exact admitted byte
digest in `owner_admission_ref`, while `evidence_refs` retains the verified
local snapshot needed for recovery and continuation closure.

The complete external-Codex deterministic suite passed. This is an ABI repair,
not new role, model-fit, owner-acceptance, publication, or external-effect
authority.

## 2026-05-07 - Initial package landing

Created the governed-execution package as the route home for governed local
worker execution, autonomy-gate reporting, return policy, candidate export, and
reviewable run records.

Validation followed the package and root validation routes.

## 2026-05-13 - Part-local runner and candidate surfaces

Kept root wrappers stable while moving governed-runner, autonomy-status,
return-policy, local-worker-path, runtime-contract, and candidate-export
surfaces into package parts with focused tests and validators.

Validation covered focused governed-execution tests, Python compilation, and
the root source route.

## 2026-05-13 - Package card completion

Added package-local `DIRECTION.md`, `PROVENANCE.md`, `ROADMAP.md`, and this
landing log so governed execution changes have explicit review and owner
handoff boundaries.

## 2026-08-01 - Source-local external Codex incarnation candidate

Added the typed `external-codex-agent` part and source wrapper. The candidate
binds one post-compile SDK incarnation to an exact role, model realization,
runtime/tool profile, task, workspace, immutable inputs, observe-only usage
metering, continuation,
and wake policy; launches a distinct Codex-compatible process; persists thread,
event, usage, argv, and result evidence; resumes the exact thread; contains
read-only drift; and requires a separately bound review before A2A export.

The paired fake-process suite passed. This entry records source-local movement,
not a deployed landing, real Luna fit, eval verdict, center integration, or
permission for any external effect.

Candidate-008 later returned separate accepted Luna max writer and reviewer
receipts. The reviewer returned the source for repair over three confirmed
contract gaps plus process/provenance warnings. The repaired deterministic
suite now covers produced-artifact admission, status-selected wake binding,
failed-reviewer export refusal, every loaded SDK module origin, and a
TERM-resistant `setsid` descendant inside parent-death containment. A live
candidate-009 run then showed that an outer user/PID namespace blocks Codex's
own bubblewrap sandbox probe. Its controlled interruption and unchanged
workspace are retained as portability counterevidence. The source now uses a
Linux subreaper supervisor without an outer namespace; a fresh real-model trial
and eval verdict remain pending.

Candidate-011 subsequently completed a real external Luna max writer pass on
the repaired transport. Independent review returned the source again: the
supervisor paid for a full procfs scan at 20 Hz while idle, and the schema-valid
writer report contained two nonexistent source references. The reviewer later
exceeded its own token ceiling, so the runtime correctly refused reviewed A2A
export. The source candidate now uses signal-notified steady-state waiting with
a bounded reap fallback, validates workspace `source:` references and anchors,
and persists post-compilation SDK import paths. These are source-local repairs;
a fresh immutable accepted writer/reviewer receipt is still required.

Candidate-012 then returned separate real Luna max and Luna xhigh writer
receipts on the same immutable packet. They exposed complementary blockers:
durable failure closeout could reread already-drifted inputs, and report
evidence schemes were not bound to stable immutable IDs or exact runtime
validation commands. The independent reviewer confirmed both classes but its
own report failed one false source-line anchor, so no accepted review or A2A
return exists. The source now persists failure closeout at admission, validates
anchored `source:` and stable `immutable:` refs plus exact
`runtime:validation:<command-id>` claims, and owns canonical non-starting
reviewer preparation instead of relying on lab scripts.

The fixed token ceilings used by earlier candidates are retained only as
methodological counterevidence: they censored agent initiative before it could
be measured. Fresh candidates count tokens, time, turns, output, and commands
under an explicit `observe_only`/no-limit policy. This is still source-local
repair, not an accepted model-fit or landing result.

Candidate-013 supplied fresh Sol max, Luna max, and Luna xhigh read-only
receipts plus a separate Luna max reviewer. It exposed mutable terminal
evidence, admission-class overstatement, and the missing post-write review
snapshot. The source now digest-binds runtime evidence, keeps
`owner_contour` unadmitted, and prepares a mutation writer's separate read-only
review from the exact final workspace manifest.

The first generated-drift mutation pair then exposed a conflation between
mutation authority and source citations plus misuse of the initial dirty
manifest as post-exit evidence. After separating `allowed_paths` from
`source_evidence_paths` and reserving a controller-owned final-manifest
identity, a fresh pair produced complementary evidence: Luna max repaired the
index but invented an immutable anchor and failed closed; Luna xhigh repaired
the same baseline, passed all fixed validations, and returned an admitted
review handoff. A distinct xhigh reviewer confirmed the repair, first failed
closed on an incarnation-ID typo, and then completed the same thread through
an explicit prior-result-digest-bound follow-up that preserved the failed
receipt. The exported A2A-compatible result binds distinct writer/reviewer
threads and exact artifact digests without waking the parent. These are useful
source/runtime observations, not model-fit, owner acceptance, deployment, or
landing completion.

The subsequent closeout run reached a semantic checkpoint and was
operator-interrupted before Codex emitted `turn.completed`. That exposed an
accounting ambiguity: zero reported tokens did not mean zero work, and ordinary
resume would overwrite the interruption receipt. The runtime now classifies
that case as a partial usage observation and preserves every prior terminal
result before exact-thread continuation. The same xhigh thread then resumed,
counted 818851 input, 652288 cached-input, and 13675 output tokens for its
observable second attempt, and retained the first attempt as a typed partial
usage gap. It substantively returned a review handoff but failed closed: the
read-only sandbox left pytest without a writable temporary directory, and two
model-authored literal source anchors did not exist. The runtime keeps anchor
validation strict. A second fresh xhigh attempt passed Git and stack validation
but again failed both pytest commands before collection despite carrying the
beta permission-profile argv. It counted 562342 input, 462848 cached-input, and
17130 output tokens over 330.976745 active seconds; its terminal model report
also failed the exact status/decision contract and is retained as
counterevidence, not a closeout result.

Current Codex 0.146.0 diagnostics narrowed the sandbox repair. Explicit
`codex sandbox -P` accepted attempt-local temp writes, whereas `codex exec`
ignored the same default-profile selection even under an isolated Codex home.
A separate execution-root probe then ran Codex's internal `workspace-write`
sandbox only over a runtime-owned cwd: tempfile creation passed and an absolute
target write failed with `EROFS`. A follow-up boundary probe also denied a
symlink-mediated write with `EROFS` and rejected hardlink/rename crossings with
`EXDEV`; it counted 68095 input, 58368 cached-input, and 2037 output tokens.
The source now records this attempt-local execution root, keeps the target
checkout outside writable roots, rejects path overlap, and leaves network
disabled. These probes establish a transport candidate only; a fresh immutable
closeout and independent review are still required.

## 2026-08-13 - Exact owner run-plan and realization admission

The external-Codex owner contour now consumes the `aoa-agents` v4 request that
explicitly repeats the SDK run-plan and selected model-realization refs. Runtime
preflight compares both with the already validated incarnation binding and
fails closed on a recomputed-request substitution. This is a runtime admission
repair; it does not move role, model-fit, plan, or owner-acceptance authority
into `abyss-stack`.
