# Runtime Repair Distillation Log

## 2026-05-07

Moved the old runtime chaos wave doc, `_v1` receipt schemas, receipt examples,
and antifragility contract test into package-local legacy.

Still legacy:

- chaos wave note
- `_v1` receipt schema filenames
- named receipt examples
- contract test with old antifragility naming

Future distillation should create quieter active names only when the repair
contract is split from wave evidence and legacy receipt IDs.
