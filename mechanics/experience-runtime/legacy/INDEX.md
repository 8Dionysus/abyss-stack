# Experience Runtime Legacy Index

## Raw Docs

Old root docs now live in `legacy/raw/`:

- adoption runtime docs
- appeal, council, veto, and governance runtime docs
- installation, first release, rollback, and operator console docs
- office mesh, office release train, handoff graph, and service mesh docs
- experience certification and deployment storage plans
- assistant release lifecycle, canary probe, constitution storage,
  rollback execution, and stay-order enforcement docs

## Artifacts

Old root artifacts now live under `legacy/artifacts/`:

- `schemas/*_v1.json` for adoption, federation harvest, governance,
  installation/release, office, storage migration, and worker records
- matching `examples/*.example.json`
- `tests/test_experience_wave*_seed_contracts.py`

## Active Bridge

Start at `../README.md`. Use these legacy files for lineage and validation, not
as the first active runtime route.

Use `ARCHIVE_CLASSIFICATION.md` to decide whether a family stays archive-only
or has enough concrete runtime ownership to promote.
