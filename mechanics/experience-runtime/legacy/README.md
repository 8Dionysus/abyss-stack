# Experience Runtime Legacy

This is the provenance home for old flat experience runtime seed files.

The files here retain their old wave, seed, and `_v1` names so history stays
auditable. The active route is the package card one level up.

Use [INDEX.md](INDEX.md) to map old locations to current locations.

