# Bootstrap

Routes bootstrap helpers and docs:
`scripts/aoa-bootstrap-configs`,
`mechanics/config-projection/parts/bootstrap/aoa_bootstrap_configs.sh`, and
`mechanics/config-projection/parts/bootstrap/docs/SECRETS_BOOTSTRAP.md`.

Bootstrap prepares runtime-local config shape from source templates; it is not a
live state capture.
