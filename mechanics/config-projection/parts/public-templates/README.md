# Public Templates

Routes public-safe runtime config templates in `config-templates/`.

This part does not absorb live `Configs/` state. Template movement must update
bootstrap, sync, and validation together.
