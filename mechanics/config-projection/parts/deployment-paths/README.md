# Deployment Paths

Routes source/runtime path contracts in `docs/runtime/PATHS.md` and
`docs/runtime/STORAGE_LAYOUT.md`.

This part keeps source checkout, runtime mirror, logs, models, and secret roots
distinct.
